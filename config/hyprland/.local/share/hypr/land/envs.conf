# $terminal = alacritty
$terminal = ghostty
# $fileManager = GTK_THEME=Catppuccin-Dark thunar
# $fileManager = GTK_THEME=adw-gtk3-dark thunar
$fileManager = $terminal -e yazi
# $menu = wofi --show drun
$menu = byte4work-walker
# $browser = firefox
# $browser = zen-browser
# $browser = google-chrome-stable
# $browser = brave
$browser = chromium
$workmsg = vesktop
$babycam = byte4work-babycam

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

#for kde apps you need to install: sudo pacman -S qt5ct qt6ct kvantum kvantum breeze-icons
#you will need to set dark theme for qt apps from kde more difficult thans with gnome :D:
env = QT_QPA_PLATFORMTHEME,qt6ct   # for Qt apps# Theme

env = QT_QPA_PLATFORM,wayland
env = GDK_BACKEND,wayland
env = MOZ_ENABLE_WAYLAND,1
env = ELECTRON_OZONE_PLATFORM_HINT,wayland

# scripts locations
env = PATH,/home/neil/.local/bin:/home/neil/.local/private/bin:/usr/local/bin:/usr/bin

# Set environment variables for applications launched within Hyprland
# This ensures Qt applications (like OBS) use the Wayland backend.
env = QT_QPA_PLATFORM,wayland

env = TERMINAL,$terminal

env = mesa_glthread,true
