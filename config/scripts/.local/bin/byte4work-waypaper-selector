#!/bin/bash

STATE_FILE="$HOME/.local/share/waypaper/default.json"
FOLDERS=(
    ~/.local/share/wallpapers
)

FOLDER_ARGS=()
for f in "${FOLDERS[@]}"; do
    FOLDER_ARGS+=("$f")
done

waypaper --state-file "$STATE_FILE" --folder "${FOLDER_ARGS[@]}"
