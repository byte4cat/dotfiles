#!/bin/bash

selected=$(cliphist list | wofi --dmenu --allow-images --prompt "Clipboard" --width 800 --height 600)

[ -z "$selected" ] && exit 0

echo "$selected" | cliphist decode | wl-copy

notify-send "Cliphist" "已複製到剪貼簿" -t 1000
